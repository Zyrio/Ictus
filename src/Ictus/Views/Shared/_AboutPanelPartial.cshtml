@{
    var version = "";
    var versionName = Ictus.Data.Constants.VersionConstant.Codename.ToString();

    if(Ictus.Data.Constants.VersionConstant.Patch == 0) {
        version = Ictus.Data.Constants.VersionConstant.Release.ToString();
    } else {
        version = Ictus.Data.Constants.VersionConstant.Release.ToString() + "." +
            Ictus.Data.Constants.VersionConstant.Patch.ToString();
    }
}

<div class="panel" id="about-panel">
    <div class="panel-inner">
        <div class="panel-close">
            <a href="#" id="about-panel-close"><i class="fa fa-fw fa-times"></i></a>
        </div>
        <h1>About</h1>
        @Html.Raw(@ViewBag.SiteDescription)
        <p>
            Powered by <strong>Ictus</strong>, version <strong>@version '@(versionName)'</strong>
        </p>
    </div>
</div>